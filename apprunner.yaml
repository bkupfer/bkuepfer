version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install conda
      - conda create --name bsite-env python=3.10
      - conda activate bsite-end
      - pip install -r requirements.txt
run:
  network:
    port: 8880
  commands: python manage.py runserver 8880
