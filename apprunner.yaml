version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "building"
      - pip install pipenv
      - pipenv install -r requirements.txt
      - echo "build successful"
run:
  network:
    port: 8880
  command: python manage.py runserver 8880
