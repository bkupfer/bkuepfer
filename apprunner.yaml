version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
    post-build:
      - python manage.py test
run:
  command: python manage.py runserver 8000
  network:
    port: 8000
